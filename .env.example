# Required Telegram API credentials
# Get these from https://my.telegram.org/apps
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here
TELEGRAM_PHONE=+1234567890

# Backup schedule (cron format: minute hour day month weekday)
# Examples:
#   0 */6 * * *    = Every 6 hours
#   0 0 * * *      = Daily at midnight
#   0 */1 * * *    = Every hour
SCHEDULE=0 */6 * * *

# Backup destination path (inside container)
BACKUP_PATH=/data/backups

# Download media files (photos, videos, documents)
# Set to false to save space and only backup text messages
DOWNLOAD_MEDIA=true

# Maximum media file size to download (in MB)
# Files larger than this will be skipped
MAX_MEDIA_SIZE_MB=100

# Chat types to backup (comma-separated)
# Options: private, groups, channels
# Examples:
#   private          = Only private conversations
#   private,groups   = Private chats and groups
#   private,groups,channels = Everything
CHAT_TYPES=private,groups,channels

# Granular chat ID filtering (optional, advanced)
# INCLUDE_CHAT_IDS: Whitelist mode - ONLY backup these specific chat IDs (comma-separated)
# EXCLUDE_CHAT_IDS: Blacklist mode - Exclude these specific chat IDs (comma-separated)
# Examples:
#   INCLUDE_CHAT_IDS=123456789,987654321   = Only backup these 2 chats
#   EXCLUDE_CHAT_IDS=555555555             = Backup everything except this chat
#   CHAT_TYPES=groups + EXCLUDE_CHAT_IDS=111,222 = All groups except chat 111 and 222
# Note: If INCLUDE_CHAT_IDS is set, it takes precedence (whitelist mode)
# INCLUDE_CHAT_IDS=
# EXCLUDE_CHAT_IDS=

# Logging level
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Session name (for multiple accounts, use different names)
SESSION_NAME=telegram_backup

# Session directory (defaults to /data/session)
# The session file is stored separately from backup data for better organization
# SESSION_DIR=/data/session
